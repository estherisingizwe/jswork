// check the problem
function login(){
    let username=prompt("enter a username:");
    let password=prompt("enter a password:");
           if ((username == 'Esther12') && (password == 'password')){
            confirm('access granted')
           }else{
              confirm('access denied');
           }
    
        }